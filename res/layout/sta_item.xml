<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@drawable/sta_bg_select"
    android:orientation="vertical" >

<!--     android:src="@drawable/ic_report_lock" -->
    <ImageView
        android:id="@+id/imageViewLock"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@id/imageViewLock"
        
        android:visibility="gone" />

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/item_margin"
        android:gravity="center_horizontal"
        android:orientation="vertical" >

        <ImageView
            android:id="@+id/imageViewIcon"
            android:layout_width="@dimen/ic_size_staicon"
            android:layout_height="@dimen/ic_size_staicon"
            android:layout_marginBottom="@dimen/MarginMini"
            android:layout_marginTop="@dimen/MarginMedium"
            android:contentDescription="@id/imageViewIcon"
            android:scaleType="centerInside" />

        <TextView
            android:id="@+id/textViewName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="@dimen/MarginMedium"
            android:text="chuanganqi"
            android:textColor="@drawable/sta_item_text_selector"
            android:textSize="16.0sp" />
    </LinearLayout>

</LinearLayout>